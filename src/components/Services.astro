---
const services = [
  {
    icon: "👜",
    title: "Accessoires",
    description: "Sacs, pochettes, trousses... Des accessoires uniques pour accompagner votre quotidien.",
    features: ["Sacs à main", "Trousses", "Pochettes", "Porte-monnaie"]
  },
  {
    icon: "🏠",
    title: "Décoration",
    description: "Embellissez votre intérieur avec des créations textiles personnalisées.",
    features: ["Coussins", "Rideaux", "Nappes", "Housses"]
  },
  {
    icon: "👶",
    title: "Puériculture",
    description: "Des créations douces et colorées pour les tout-petits.",
    features: ["Bavoirs", "Doudous", "Tours de lit", "Gigoteuses"]
  },
  {
    icon: "🎁",
    title: "Sur mesure",
    description: "Une idée spéciale ? Je réalise vos projets personnalisés.",
    features: ["Cadeaux", "Événements", "Projets uniques", "Retouches"]
  }
];
---

<section id="services" class="services section">
  <div class="container">
    <span class="section-badge">Mon univers</span>
    <h2 class="section-title">Ce que je crée</h2>
    <p class="section-subtitle">
      Des créations artisanales dans différents domaines, 
      toujours avec le même amour du détail et de la beauté.
    </p>

    <div class="services-grid">
      {services.map((service, index) => (
        <div class="service-card" style={`--delay: ${index * 0.1}s`}>
          <div class="service-icon">{service.icon}</div>
          <h3 class="service-title">{service.title}</h3>
          <p class="service-description">{service.description}</p>
          <ul class="service-features">
            {service.features.map(feature => (
              <li>
                <span class="check">✓</span>
                {feature}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <div class="process-section">
      <h3 class="process-title">Comment ça marche ?</h3>
      <div class="process-steps">
        <div class="step">
          <div class="step-number">1</div>
          <h4>Contact</h4>
          <p>Vous me décrivez votre projet et vos envies</p>
        </div>
        <div class="step-line"></div>
        <div class="step">
          <div class="step-number">2</div>
          <h4>Devis</h4>
          <p>Je vous propose un devis personnalisé</p>
        </div>
        <div class="step-line"></div>
        <div class="step">
          <div class="step-number">3</div>
          <h4>Création</h4>
          <p>Je réalise votre pièce avec soin</p>
        </div>
        <div class="step-line"></div>
        <div class="step">
          <div class="step-number">4</div>
          <h4>Livraison</h4>
          <p>Vous recevez votre création unique</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .services {
    background: var(--white);
  }

  .section-badge {
    display: block;
    text-align: center;
    background: var(--secondary);
    color: var(--primary);
    padding: 8px 20px;
    border-radius: 50px;
    font-size: 0.9rem;
    font-weight: 500;
    margin: 0 auto 20px;
    width: fit-content;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 30px;
  }

  .service-card {
    background: var(--light);
    padding: 35px 25px;
    border-radius: 20px;
    text-align: center;
    transition: all 0.3s ease;
    border: 2px solid transparent;
  }

  .service-card:hover {
    transform: translateY(-10px);
    border-color: var(--primary);
    box-shadow: var(--shadow-lg);
  }

  .service-icon {
    font-size: 3rem;
    margin-bottom: 20px;
  }

  .service-title {
    font-size: 1.3rem;
    margin-bottom: 15px;
    color: var(--dark);
  }

  .service-description {
    color: var(--accent);
    font-size: 0.95rem;
    margin-bottom: 20px;
    line-height: 1.6;
  }

  .service-features {
    list-style: none;
    text-align: left;
  }

  .service-features li {
    padding: 8px 0;
    color: var(--accent);
    font-size: 0.9rem;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .check {
    color: var(--primary);
    font-weight: bold;
  }

  .process-section {
    margin-top: 80px;
    padding: 60px;
    background: linear-gradient(135deg, var(--secondary) 0%, rgba(212, 165, 116, 0.3) 100%);
    border-radius: 30px;
  }

  .process-title {
    text-align: center;
    font-size: 2rem;
    margin-bottom: 50px;
    color: var(--dark);
  }

  .process-steps {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    gap: 20px;
  }

  .step {
    text-align: center;
    flex: 1;
    max-width: 200px;
  }

  .step-number {
    width: 50px;
    height: 50px;
    background: var(--primary);
    color: var(--white);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Playfair Display', serif;
    font-size: 1.3rem;
    font-weight: 700;
    margin: 0 auto 15px;
  }

  .step h4 {
    font-family: 'Poppins', sans-serif;
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 10px;
    color: var(--dark);
  }

  .step p {
    color: var(--accent);
    font-size: 0.9rem;
  }

  .step-line {
    width: 60px;
    height: 2px;
    background: var(--primary);
    margin-top: 25px;
    opacity: 0.5;
  }

  @media (max-width: 1100px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .services-grid {
      grid-template-columns: 1fr;
    }

    .process-section {
      padding: 40px 20px;
    }

    .process-steps {
      flex-direction: column;
      align-items: center;
    }

    .step-line {
      width: 2px;
      height: 30px;
      margin: 0;
    }
  }
</style>
